<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sv.gauge">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.SET_DEBUG_APP"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- login activity -->
        <activity
            android:name=".LoginActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- main activity -->
        <activity
            android:name=".MainActivity"
            android:screenOrientation="portrait"></activity>

        <!-- domain select activity -->
        <activity
            android:name=".DomainSelectActivity"
            android:screenOrientation="portrait" />

        <!-- map activity -->
        <activity
            android:name=".MapActivity"
            android:screenOrientation="portrait" />

        <!-- domain step activity -->
        <activity
            android:name=".DomainStepActivity"
            android:screenOrientation="portrait" />

        <!-- transaction report list activity -->
        <activity
            android:name=".TransactionReportListActivity"
            android:screenOrientation="portrait" />

        <!-- transaction report activity -->
        <activity
            android:name=".TransactionReportActivity"
            android:screenOrientation="portrait" />

        <!-- test -->
        <activity android:name=".Test">

            <!-- <intent-filter> -->
            <!-- <action android:name="android.intent.action.MAIN" /> -->


            <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
            <!-- </intent-filter> -->
        </activity>
        <activity android:name=".SyncActivity"></activity>
    </application>

</manifest>
